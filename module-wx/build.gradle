apply from: "../module.build.gradle"

android {

    defaultConfig {
        if (isBuildModule.toBoolean()) {
            applicationId "com.dashingqi.wx"
        }
    }

    //统一资源前缀，规范资源的引用
    resourcePrefix "wx_"
}

dependencies {

}
